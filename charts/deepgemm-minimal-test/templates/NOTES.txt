DeepGEMM Minimal Test deployed!

  Test ID:   {{ .Values.testId }}
  Namespace: {{ .Values.namespace }}
  Node:      {{ (index .Values.topology.nodes 0).name }}
  Version:   {{ .Values.deepgemm.version }}

Monitor:
  kubectl logs -n {{ .Values.namespace }} -l app=deepgemm-minimal-test,test-id={{ .Values.testId }} -f

Cleanup:
  helm uninstall {{ .Release.Name }} -n {{ .Values.namespace }}
