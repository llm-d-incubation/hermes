# Example environment configuration for Hermes
# Copy this file to .env and update with your actual values

# CoreWeave cluster configuration
# Path to your CoreWeave kubeconfig file
COREWEAVE_KUBECONFIG=~/path/to/your/coreweave-kubeconfig

# OpenShift cluster configuration (if behind corporate proxy)
# HTTP(S) proxy for cluster access
OPENSHIFT_HTTPS_PROXY=http://your-proxy-ip:port

# GKE cluster configuration
# No special env vars needed - uses current kubectl context
# Run: gcloud container clusters get-credentials CLUSTER_NAME
